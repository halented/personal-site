import { redirect } from 'next/navigation';

export default function Page() {
    redirect('/beamsaber')
}

// maybe someday we will update this to a searchable list of users, so people can find their game's beamsaber timers without having to have their GM's specific url